# BddJavaUIFramework
BddJavaGuiFramework

Prerequisites

You must have installed following app:

    Java JDK 11+

build the project :

    ./gradlew build

Set environment.json with valid credentials

    "username": {email}
    "password": {password}

Run all test :

    ./gradlew runFeatures
